# Core — PyTorch with CUDA (install separately, see README)
# pip install torch --index-url https://download.pytorch.org/whl/cu121

# Data pipeline
datasets>=2.14.0
huggingface-hub>=0.19.0
tokenizers>=0.15.0
numpy>=1.24.0

# Data quality filtering
langdetect>=1.0.9

# Training monitoring (optional)
wandb>=0.16.0
psutil>=5.9.0

# Optimizer alternatives (optional)
bitsandbytes>=0.41.0        # 8-bit AdamW — saves ~1.3GB VRAM
muon-pytorch>=0.1.0         # Muon optimizer — alternative to AdamW

# Linux only (optional — enables torch.compile for ~20% speedup)
# triton>=2.1.0