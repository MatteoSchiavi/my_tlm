# ═══════════════════════════════════════════════════════════════
# My_SLM v4.0 — Requirements
# ═══════════════════════════════════════════════════════════════
#
# Install PyTorch FIRST (the default pip gives CPU-only!):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install -r requirements.txt
#
# ═══════════════════════════════════════════════════════════════

# ─── Core (required) ─────────────────────────────────────────
torch>=2.1.0              # MUST install with CUDA: --index-url https://download.pytorch.org/whl/cu121
numpy>=1.24
tokenizers>=0.15
datasets>=2.14
huggingface-hub>=0.19

# ─── Data Quality (required) ─────────────────────────────────
langdetect>=1.0.9         # Language detection for Italian/English filtering
datasketch>=1.6           # MinHash LSH fuzzy deduplication (v4.0)

# ─── Optional: Experiment Tracking ───────────────────────────
wandb>=0.16             # Weights & Biases logging (enable: WANDB=1 python train.py)
psutil>=5.9             # System metrics for WandB