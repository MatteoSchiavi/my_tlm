# Core dependencies — required for the full pipeline
torch>=2.1.0
numpy>=1.24.0
tokenizers>=0.15.0
datasets>=2.14.0
huggingface_hub>=0.19.0

# Data download — required for download.py
requests>=2.28.0

# Quality filtering — used by filter.py for fuzzy dedup
datasketch>=1.6.0

# Fast JSON parsing — significantly speeds up filter.py, mix.py, preprocess.py (3-10x)
orjson>=3.9.0

# Language detection — used by download.py and filter.py for Italian identification
langdetect>=1.0.9

# System monitoring — used across pipeline for RAM monitoring
psutil>=5.9.0

# 8-bit optimizer — saves ~1.32 GB VRAM during training (optional but recommended)
bitsandbytes>=0.41.0

# Experiment tracking — optional, enable with WANDB=1
wandb>=0.15.0

# Triton — required for torch.compile inductor backend (auto-installed with PyTorch on Linux)
# Not available on Windows; training falls back to eager mode automatically.
# triton>=2.1.0

# Muon optimizer — optional alternative optimizer, enable with MUON=1
# muon>=0.1.0
